# Supabase Configuration
# =======================
# Estas variables son necesarias para conectar la aplicación con tu proyecto de Supabase.
# Copia este archivo a .env y reemplaza los valores con tus credenciales reales.

# URL de tu proyecto de Supabase
# Formato: https://[tu-proyecto-id].supabase.co
# Dónde encontrarlo: Dashboard de Supabase -> Settings -> API -> Project URL
VITE_SUPABASE_URL=https://tu-proyecto-id.supabase.co

# Anon Key (Clave pública) de tu proyecto de Supabase
# Esta clave es segura para usar en el cliente ya que las operaciones están protegidas por RLS
# Dónde encontrarlo: Dashboard de Supabase -> Settings -> API -> Project API keys -> anon/public
VITE_SUPABASE_ANON_KEY=tu-anon-key-aqui

# Notas Importantes:
# ------------------
# 1. Las variables DEBEN tener el prefijo VITE_ para estar disponibles en el cliente
# 2. Nunca subas el archivo .env al repositorio (está en .gitignore)
# 3. La seguridad se maneja mediante Row Level Security (RLS) en Supabase
# 4. Después de modificar este archivo, reinicia el servidor de desarrollo
